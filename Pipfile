[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
server = "uvicorn main:app --reload --port 12000"

[packages]
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}
twilio = "*"

[dev-packages]

[requires]
python_version = "3.10"
